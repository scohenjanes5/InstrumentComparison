Cost function C(w,b)=1/2n Sum_x||(y(x)-a)|^2

w are weights
b are biases
n number of training inputs
a vector of outputs when x is an inputs
sum over all training inputs x

minimized when low error
use gradient decent

